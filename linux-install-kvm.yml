---
- hosts: all
  gather_facts: no
  validate_certs: no

  tasks:
  - name: Add default tools for kvm
    dnf:
      name: "{{ packages }}"
      state: present
    vars:
      packages:
        - htop
        - tcpdump
        - nano
        - wireshark
